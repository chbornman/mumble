So basically what happened was, I pushed the commit to main instead of the feature branch, and then I realized the pre-commit hook didn't run because I used the no-verify flag. So now we've got untested code in production. I'm going to revert the commit, cherry-pick it onto the correct branch, and then open a proper pull request. Lesson learned, always double-check your branch before pushing.